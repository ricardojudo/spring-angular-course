package com.example.springboot.rest;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import com.example.springboot.posts.Post;
import com.example.springboot.posts.PostService;

@RestController
/*
 * Exponer API en ruta /api/posts
 */
public class PostsController {

	/*
	 * Definir referencia a PostService
	 */
	
	
	/*
	 * Generar metodo index
	 */
	
	
	/*
	 * Generar metodo show
	 */
	
	
	/*
	 * Generar metodo post
	 */
	
	
	/*
	 * Generar metodo update
	 */
	
	
	/*
	 * Generar metodo delete
	 */
	
}
